import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        backgroundColor: "#FFFFFF",
        borderRadius: 10,
        padding: 15,
        alignItems: "center"
    },
    text: {
        color: "#030405",
        fontSize: 20,
        fontWeight: "bold"
    },
})
