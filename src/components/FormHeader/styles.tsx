import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        width: "100%",
        maxHeight: "10%"
    },
    button:{
        flexDirection: "row",
        alignItems: "center"
    },
    text:{
        fontSize: 20,
        fontWeight: "bold"
    }
})
